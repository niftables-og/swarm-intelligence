export const config = {
  socialMedia: {
    x: process.env.NEXT_PUBLIC_X_URL || '',
    telegram: process.env.NEXT_PUBLIC_TELEGRAM_URL || '',
    joinNow: process.env.NEXT_PUBLIC_JOIN_NOW_URL || '',
  },
}
